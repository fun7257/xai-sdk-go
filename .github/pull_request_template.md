## Summary

<!-- What changed and why (user-facing impact). -->

## Checklist

- [ ] Offline gates green (`make check` or CI) — see `CONTRIBUTING.md`
- [ ] No API keys, tokens, or secrets in the diff
- [ ] `CHANGELOG.md` updated for user-visible changes
- [ ] Public API / Go UX → `docs/PARITY.md` / `docs/DIFF.md` as needed
- [ ] Stability policy → `docs/COMPATIBILITY.md` if changed
- [ ] Prefer Go-first entry points (`Sample`/`Samples`, validating tools)

## Test plan

<!-- How you verified (unit tests, `go test -run Example`, manual steps). -->

## Notes

<!-- Optional: screenshots, follow-ups, related issues. -->
