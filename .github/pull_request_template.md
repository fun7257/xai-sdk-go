## Summary

<!-- What changed and why (user-facing impact). -->

## Checklist

- [ ] `make test` / `make vet` / `make examples` pass (or CI green)
- [ ] No API keys, tokens, or secrets in the diff
- [ ] `CHANGELOG.md` updated for user-visible changes
- [ ] If public API / Go UX changed: update `docs/DIFF.md` or `docs/PARITY.md`
- [ ] If stability policy changed: update `docs/COMPATIBILITY.md`
- [ ] Prefer Go-first entry points (`Sample`/`Samples`, validating tool constructors)

## Test plan

<!-- How you verified (unit tests, `go test -run Example`, manual steps). -->

## Notes

<!-- Optional: screenshots, follow-ups, related issues. -->
